# Gemstone Code

Gemstone Code is a React single-page application served by Vite.

## Get started

Install dependencies:

```bash
npm install
```

Start the Vite development server:

```bash
npm run dev
```

Create a production build:

```bash
npm run build
```

The browser entry point is `src/main.jsx`.
